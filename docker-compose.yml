version: "3.2"

services:
  dev:
    container_name: appchain-create
    image: paritytech/ci-linux:974ba3ac-20201006
    working_dir: /var/www/appchain-create
    ports:
      - "9944:9944"
    environment:
      - CARGO_HOME=/var/www/appchain-create/.cargo
    volumes:
      - .:/var/www/appchain-create
      - type: bind
        source: ./.local
        target: /root/.local
    command: bash -c "cargo build --release && ./target/release/appchain-create --dev --ws-external"
